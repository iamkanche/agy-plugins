# Tech — Guidelines

<!-- written by /sdd:init -->

- **Backend:** Go 1.22 (use `any`, not `interface{}`).
- **Frontend:** TypeScript + React.
- **DB:** PostgreSQL 15.
- **Auth:** session cookies today; SSO planned (see feature `login`).
