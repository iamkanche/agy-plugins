{
  "name": "git",
  "version": "0.1.0",
  "description": "Git commands for Antigravity (git): commit, push, branch-create, branch-delete, tag-create, status, fetch, pull, stash, rebase, switch. Side effects require explicit confirmation.",
  "author": { "name": "iamkanche" },
  "rules": [
    "rules/output-language.md",
    "rules/git-hard-rules.md"
  ],
  "agents": [
    "agents/git-operator/agent.json"
  ]
}
