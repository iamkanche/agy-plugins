# Rules — Guidelines

<!-- written by /sdd:init; hand-tuned -->

- Never work on `main` / `develop`; branch first.
- Never commit secret files (credentials, local env config).
- Every acceptance criterion must be testable.
- Cite sources in research; mark guesses as 推測.
