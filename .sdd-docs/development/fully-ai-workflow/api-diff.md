# API Diff: fully-ai-workflow

No API changes.

This feature modifies only markdown documentation files (policy documents and skill
definitions). It introduces no new endpoints, modifies no existing endpoints, and removes no
endpoints. There are no HTTP, RPC, or event-stream interface changes.
