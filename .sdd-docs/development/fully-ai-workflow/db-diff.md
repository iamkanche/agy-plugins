# DB Diff: fully-ai-workflow

No schema changes.

This feature modifies only markdown documentation files. It introduces no new config keys,
tables, columns, or data structures. The existing `settings.json` key `"mode": "auto"` is
already present and requires no modification.
