# Task Manifest: SDD Skills & Workflow Map

- [x] **Task 1: Add Workflow & Skills Map Data Model**
  - Add `sddWorkflowPhases` data array to `index.html` script.
  - Register `skills-map` entry in `pluginsData` dictionary.
- [x] **Task 2: Implement Map Styles and CSS Flow Layout**
  - Add CSS classes for `.workflow-map-container`, `.phase-step-card`, `.phase-step-header`, `.skill-flow-connector`, `.skill-node-pill`, `.agent-tag-pill`, and transition effects.
- [x] **Task 3: Implement Dynamic Rendering Logic**
  - Update `renderCommands()` function in `index.html` to detect `isMapView` and render the interactive phase flow graph.
  - Implement search filter logic for skill nodes and phase descriptions in map view.
- [x] **Task 4: Audit & Local Verification**
  - Verify HTML structure, CSS responsiveness, JS execution, and link targets.
